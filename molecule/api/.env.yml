---
components:
  - name: openwebui
    path: playbook.yml
    parameters:
      api_credentials_secret: foo bar
      expose_api: true
      model: "" # don't pull a model in CI
      workspace_fqdn: localhost
